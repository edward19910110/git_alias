[user]
    email = edward.chenyc@gmail.com
    name = Edward Chen
[alias]
    st = status
    co = checkout
    br = branch
    cp = cherry-pick
    lg = log --graph --decorate --pretty=oneline --abbrev-commit --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n''          %C(white)%s%C(reset) %C(dim white)- %an%C(reset)-'
    df = diff
#   lg = log --graph --decorate --pretty=oneline --abbrev-commit
[color]
    ui = true
#[http]
#   postBuffer = 1024000000
[color "status"]
    header = bold blue
    branch = bold yellow
[core]
    editor = vim 
    packedGitLimit = 1g
    packedGitWindowSize = 1g
[pack]
    threads = 1 
    deltaCacheSize = 1g
    packSizeLimit = 1g
    windowMemory = 1g
